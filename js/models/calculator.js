export default function Calculator() {}

Calculator.prototype.plus = function (number1, number2) {
  return number1 + number2;
};

Calculator.prototype.times = function (number1, number2) {
  return number1 * number2;
};
